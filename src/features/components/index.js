import Error, { ErrorPage } from './Error';
import Header from './Header';
import Loader, { LoaderPage } from './Loader';
import NotFound, { NotFoundPage } from './NotFound';

export { Error, NotFound, NotFoundPage, ErrorPage, LoaderPage, Header, Loader };
